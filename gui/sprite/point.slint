import { PointInfo } from "../info.slint";
import { GroupableSprite } from "groupable.slint";

export component Point inherits GroupableSprite {
    // Required
    in property <int> index;

    // Model – Implemented
    model_x: PointInfo.x(self.index);    
    model_y: PointInfo.y(self.index);    
}

